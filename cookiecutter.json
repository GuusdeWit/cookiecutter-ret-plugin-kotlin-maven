{
	"pluginName": "",
	"description": "",
	"hyphenated": "{{ '-'.join(cookiecutter['pluginName'].lower().split()).replace('_', '-') }}",
	"pluginClassName": "{{ cookiecutter['hyphenated'].replace('-', ' ').title().replace(' ', '') }}"
}
